apply plugin: 'osgi'
apply plugin: 'com.zyxist.chainsaw'

description = 'Apache Jena - RDF Connection'

ext {
    javaModuleName = 'org.apache.jena.rdfconnection'
}

javaModule {
    name = javaModuleName
}

dependencies {
    compile project(':jena.arq')
    testCompile project(path: ':jena.base', configuration: 'testOutput')
    testCompile project(path: ':jena.core', configuration: 'testOutput')
}

task packageTests(type: Jar) {
    from sourceSets.test.output
    classifier = 'tests'
}

artifacts.archives packageTests

jar {
    manifest {
        description project.description
        docURL project.docURL
        vendor project.vendor
        license project.license

        instruction 'Automatic-Module-Name', javaModuleName
        instruction 'Import-Package', '*'
        instruction 'Export-Package', "$javaModuleName;version=$projectOsgiVersion"
    }
    appendix 'jpms'
}